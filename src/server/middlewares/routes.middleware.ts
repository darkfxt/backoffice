import { NextFunction, Request, Response } from 'express';
import { RoutesService } from '../api/services/routes.service';

export class RoutesMiddleware {

  checkIfExist(req: Request, res: Response, next: NextFunction) {

  }
}
