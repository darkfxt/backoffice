import { Input } from '@angular/core';

export interface ListItemInterface {
  data: any;
  selectionMode?: boolean;
  // image: any;
  // title: string;
  // subtitle: string;
  // description: string;
}
