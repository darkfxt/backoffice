import { CommonListDirective } from './common-list.directive';

describe('CommonListDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonListDirective();
    expect(directive).toBeTruthy();
  });
});
